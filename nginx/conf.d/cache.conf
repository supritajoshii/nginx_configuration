# Shared disk cache for public/proxyable HTTP responses.
#
# Cache metadata lives in 100 MB shared memory.
# Cached response bodies can use up to 2 GB on disk.
proxy_cache_path /var/cache/nginx
    levels=1:2
    keys_zone=django_cache:100m
    max_size=2g
    inactive=60m
    use_temp_path=off;
